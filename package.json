{
  "private": true,
  "type": "module",
  "scripts": {
    "test:social": "firebase emulators:exec --project demo-secret-library --only firestore \"node --test tests/social-rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.0"
  }
}
